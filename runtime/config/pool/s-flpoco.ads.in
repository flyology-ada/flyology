package System.Flyology.Pool_Config is
   pragma Preelaborate;

   --  Generated by prepare-rts.sh from validated build-time settings.  Keep
   --  this unit scalar-only: the scheduler reads it before any event loop has
   --  been created, and configuration queries must preserve lazy startup.
   Automatic_Pool_Size : constant Positive := @AUTOMATIC_POOL_SIZE@;
end System.Flyology.Pool_Config;
