name = "adaptive_pool_conformance"
description = "TLA+ conformance replay for the adaptive allocation-pool lifecycle"
version = "0.1.0-dev"

authors = ["Flyology maintainers"]
maintainers = ["Flyology maintainers <noreply@example.com>"]
maintainers-logins = ["yrashk"]
licenses = "MIT OR Apache-2.0"
tags = ["ada", "tla", "conformance", "test"]
project-files = ["adaptive_pool_conformance.gpr"]
executables = ["adaptive-pool-conformance"]

[build-switches]
"*" = { Compile_Checks = "warnings" }

[[depends-on]]
flyology = "~0.2.0"

[[depends-on]]
flyology_tla = "~0.1.0-dev"
