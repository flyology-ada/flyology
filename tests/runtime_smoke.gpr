with "../gnatevl.gpr";

project Runtime_Smoke is
   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Object_Dir use "obj/runtime_smoke";
   for Exec_Dir use "bin";
   for Main use
     ("connection_lifecycle_smoke.adb",
      "execution_groups_smoke.adb",
      "fairness_smoke.adb",
      "files_smoke.adb",
      "io_smoke.adb",
      "io_starvation_smoke.adb",
      "lifecycle_smoke.adb",
      "runtime_smoke.adb",
      "timer_heap_smoke.adb",
      "tcp_native_smoke.adb");
   for Create_Missing_Dirs use "True";

   package Compiler is
      for Default_Switches ("Ada") use
        ("-gnat2022", "-gnata", "-gnatVa", "-gnatwa");
   end Compiler;
end Runtime_Smoke;
